#include <iostream>
#include <iomanip>
using namespace std;

int main() {

int x=10,y=20;

cout<<(x<y)<<"\n"
<<(x>y)<<"\n"               //these will show 1 when true and 0 if negative
<<(x==y)<<"\n"          // note: == is comparision where as = is assignment operator
<<(x>=y)<<"\n"
<<(x<=y)<<"\n"
<<(x!=y)<<"\n";


	return 0;
}
